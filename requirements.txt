# =====================================================================
# nlp_quantum_hub — Python requirements
# Tested on Python 3.10 / macOS arm64
# Install with: pip install -r requirements.txt
# =====================================================================

# --- Core scientific stack ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13

# --- DisCoCat / QNLP ---
# lambeq pulls in discopy
lambeq>=0.4.3,<0.6

# --- Quantum simulator ---
pennylane>=0.34
pennylane-lightning>=0.34

# --- Deep learning (BiLSTM baseline + PyTorchModel quantum training) ---
torch>=2.1

# --- NLP utilities ---
nltk>=3.8

# --- Data download & I/O ---
requests>=2.31
datasets>=2.18

# --- Progress bar + config ---
tqdm>=4.66
pyyaml>=6.0

# --- Image / diagram rendering (lambeq) ---
pillow>=10.0

# --- Slides (optional — for regenerating papers/lncs/slides.pptx) ---
python-pptx>=1.0
